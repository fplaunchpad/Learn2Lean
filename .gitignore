# Lake build artifacts and downloaded packages
.lake/

# Local editor / tooling settings (do not commit)
.claude/
.vscode/
.idea/

# Note: lake-manifest.json is intentionally committed (tracked) so that
# `lake exe cache get` resolves the exact Mathlib revision and forkers
# get prebuilt oleans instead of compiling Mathlib from source.
